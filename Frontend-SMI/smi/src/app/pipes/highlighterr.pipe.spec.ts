import { HighlighterrPipe } from './highlighterr.pipe';

describe('HighlighterrPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlighterrPipe();
    expect(pipe).toBeTruthy();
  });
});
